{
  "name": "constructerp",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w @constructerp/api",
    "dev:web": "npm run dev -w @constructerp/web",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "db:migrate": "npm run db:migrate -w @constructerp/api",
    "db:seed": "npm run db:seed -w @constructerp/api",
    "db:studio": "npm run db:studio -w @constructerp/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
